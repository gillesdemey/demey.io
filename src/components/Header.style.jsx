import { css } from '@emotion/core'

const base = css`
  font-size: 1.5em;
  font-family: 'Montserrat';
  font-weight: 900;

  color: black;

  > a { color: black; text-decoration: none; }
`

export {
  base
}
