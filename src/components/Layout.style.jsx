import { css } from '@emotion/core'

const base = css`
  max-width: 42rem;
  margin: 0 auto;
`

const wrapper = css`
  p {
    font-family: 'Merriweather';
    font-size: 1em;
  }
`

export {
  base, wrapper
}
